apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-configmap
  namespace: instapay-dev
data:
  DB_USER:                   admin
  DB_HOST:                   postgres
  DB_PORT:                   "5432"
  DB_NAME_USER_SERVICE:      user-service-dev
  ENV:                       dev
